# Langage Javascript

> ❌ A travailler

> ✔️ Auto validation par l'étudiant

## 🎓 J'ai compris et je peux expliquer

- les `structures` de base du langage ✔️
- 
- Variables : JavaScript utilise les mots-clés let ou const pour déclarer des variables. Par exemple :
let x = 5; // Déclaration d'une variable 'x' avec la valeur 5
const PI = 3.14; // Déclaration d'une constante 'PI' avec la valeur 3.14

- les normes `ecmascript`  ✔️
- 
- ECMAScript 5 (ES5) : Publiée en 2009, cette version apporte de nombreuses améliorations à JavaScript, y compris de nouvelles méthodes pour les tableaux, le support des propriétés getters/setters, etc.

ECMAScript 6 (ES6) / ECMAScript 2015 (ES2015) : Publiée en 2015, cette version introduit des fonctionnalités majeures telles que les classes, les modules, les fonctions fléchées, les promesses, les destructurations, etc.

ECMAScript 2016 (ES2016) et versions ultérieures : Depuis ES6, ECMAScript suit un rythme de publication annuelle avec des fonctionnalités ajoutées chaque année. Ces versions ultérieures comprennent des améliorations telles que l'opérateur de repos, les fonctions async/await, les améliorations des objets littéraux, etc

- l'utilisation de l'`asynchrone` ✔️
- 
- JavaScript prend en charge les opérations asynchrones grâce aux fonctions asynchrones et aux promesses. L'asynchronisme permet d'exécuter des opérations sans bloquer l'exécution du reste du code. Cela est utile pour des tâches telles que les appels API, les opérations de fichiers, etc.

L'utilisation de l'asynchrone peut se faire de différentes manières, notamment :

Callbacks : Les callbacks sont des fonctions qui sont passées comme arguments à d'autres fonctions et qui sont exécutées une fois qu'une opération asynchrone est terminée.

Promesses : Les promesses sont des objets qui représent

- les spécifités du mot-clef `this` ✔️

- Le mot-clé this en JavaScript est utilisé pour faire référence à l'objet courant sur lequel une fonction est appelée. Son comportement dépend du contexte dans lequel la fonction est utilisée. Voici un résumé des principales spécificités :

Lorsqu'une fonction est appelée en tant que méthode d'un objet, this fait référence à l'objet lui-même.
Dans le contexte global, this fait référence à l'objet global, généralement window dans les navigateurs.
Les fonctions fléchées ne lient pas leur propre this et conservent le this du contexte englobant.
Les méthodes call, apply ou bind permettent de spécifier explicitement le this lors de l'appel d'une fonction.
Il est important de comprendre que le comportement de this peut varier en fonction du contexte, ce qui peut parfois conduire à des résultats inattendus si on ne fait pas attention à sa manipulation.

## 💻 Je code en Javascript

### Un exemple de code commenté  ✔️

// Déclaration d'une variable numérique
let nombre = 10;

// Affichage de la valeur de la variable
console.log(nombre); // Affiche 10

// Incrémentation de la variable
nombre++;

// Affichage de la nouvelle valeur de la variable
console.log(nombre); // Affiche 11

// Déclaration d'une variable de type chaîne de caractères
let message = "Hello, world!";

// Affichage du message
console.log(message); // Affiche "Hello, world!"

// Concaténation de deux chaînes de caractères
message += " Welcome to JavaScript!";

// Affichage du nouveau message
console.log(message); // Affiche "Hello, world! Welcome to JavaScript!"

### Utilisation dans un projet ❌ / ✔️

[[lien github](...)](https://github.com/AlexisFaugeroux/wild-carbon/tree/develop)

Description : Projet de d'évaluation carbone codé en javaScript TypeScript

### J'ai utilisé ce langage en production ❌ / ✔️

https://github.com/AlexisFaugeroux/wild-carbon/tree/develop

Description :

### J'ai utilisé ce langage en environement professionnel ❌ / ✔️

Description :

## 🌐 J'utilise des ressources
DevDocs JavaScript
W3Schools JavaScript Tutorial
JavaScript.info 
MDN JavaScript Guide 
### Titre

- lien
- description

## 🚧 Je franchis les obstacles

### Point de blocage ❌ / ✔️

Description:

Plan d'action : (à valider par le formateur)

- action 1 ❌ / ✔️
- action 2 ❌ / ✔️
- ...

Résolution :

## 📽️ J'en fais la démonstration

- J'ai ecrit un [tutoriel](...) ✔️
- J'ai fait une [présentation](...) ❌ / ✔️

